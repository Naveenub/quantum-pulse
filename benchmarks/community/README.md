# Community Benchmarks

Submit your benchmark results here via a pull request.

**How to contribute:**
```bash
python scripts/benchmark_compare.py --records 1000 --output results.json
```

Then open a PR adding:
- `benchmarks/community/YOUR_DESCRIPTION.json` — the raw results
- A line in this README with your hardware, OS, and corpus type
